export type AccountDTO = {
  puuid: string;
  gameName?: string;
  tagLine?: string;
};

export type SummonerDTO = {
  profileIconId: number;
  revisionDate: number;
  puuid: string;
  summonerLevel: number;
};
